import React from "react";

export default function PatientDetail(item) {
  return (
    <div
      className="container"
      style={{
        width: "100%",
        width: "100%",
        borderRadius: "25px",
        background: "#5ABF84",
      }}
    >
      <div>
        <h3>Patient Details</h3>
      </div>
    </div>
  );
}
